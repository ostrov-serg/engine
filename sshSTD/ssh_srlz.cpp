
#include "stdafx.h"
#include "ssh_srlz.h"

namespace ssh
{
	void ssh::Serialize::open(const Buffer & buf, void * srlz, bool is_xml)
	{
	}

	void ssh::Serialize::save(const String & path, void * srlz, bool is_xml)
	{
	}

	/*
	void ssh::Serialize::readXml(HXML hp, ssh_l p_offs, ssh_l idx)
	{
	}

	void ssh::Serialize::writeXml(HXML h, ssh_l p_offs)
	{
	}
	*/
	void ssh::Serialize::writeBin(ssh_l p_offs)
	{
	}

	void ssh::Serialize::readBin(ssh_l p_offs)
	{
	}
}
